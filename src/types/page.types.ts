export type PublicPageConfig = {
  eyebrow: string;
  title: string;
  description: string;
};

export type ExamplePageConfig = {
  eyebrow: string;
  title: string;
  description: string;
};

export type ComponentPageConfig = {
  eyebrow: string;
  title: string;
  description: string;
};
